#ifndef _DOOM_SHIM_STRINGS_H
#define _DOOM_SHIM_STRINGS_H

// POSIX strings.h (distinct from string.h) -- doomtype.h only wants
// str[n]casecmp from it (see string.h shim, which already declares both).
#include <string.h>

#endif
